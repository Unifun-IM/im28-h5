{
  "name": "im28-h5-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "assets:sync": "node scripts/sync-rn-assets.mjs",
    "assets:check": "node scripts/sync-rn-assets.mjs --check",
    "sdk:build:web": "npm --prefix ../im28-sdk run build:web",
    "build": "npm run sdk:build:web && npm run build -w @im28/h5-web",
    "dev": "npm run dev:pc",
    "dev:pc": "npm run dev:pc -w @im28/h5-web",
    "dev:h5": "npm run dev:h5 -w @im28/h5-web",
    "smoke:gateway": "npm run sdk:build:web && node scripts/gateway-runtime-smoke.mjs",
    "test": "npm --prefix ../im28-sdk run test:web",
    "typecheck": "npm run sdk:build:web && npm --prefix ../im28-sdk run typecheck:web && npm run typecheck -w @im28/h5-web",
    "verify": "npm run assets:check && npm run typecheck && npm test && npm run build"
  },
  "engines": {
    "node": ">=22.11.0"
  }
}
