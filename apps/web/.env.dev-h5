# 与 im28-phone_2/src/config/appEnvironment.ts 的 DEV_ENDPOINTS 保持一致。
VITE_GATEWAY_HTTP_URL=https://im-dev-api-gateway.djftech.app/
VITE_GATEWAY_WS_URL=wss://im-dev-push-gateway.djftech.app/ws

# H5 路径调试显式使用新版 Gateway OpenAPI 的 h5 平台。
VITE_GATEWAY_PLATFORM=pc
